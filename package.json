{
  "name": "zerodha-clone",
  "private": true,
  "scripts": {
    "start": "concurrently -n backend,frontend,dashboard -c yellow,green,blue \"npm run start:backend\" \"npm run start:frontend\" \"npm run start:dashboard\"",
    "start:backend": "npm --prefix backend start",
    "start:frontend": "npm --prefix frontend start",
    "start:dashboard": "npm --prefix dashboard start",
    "build": "npm run build:frontend && npm run build:dashboard",
    "build:frontend": "npm --prefix frontend run build",
    "build:dashboard": "npm --prefix dashboard run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "react-scripts": "5.0.1"
  }
}
